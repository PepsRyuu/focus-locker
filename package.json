{
    "name": "focus-locker",
    "version": "0.2.0",
    "description": "Simple utility for locking focus to an element.",
    "main": "dist/FocusLocker.umd.js",
    "author": "Paul Sweeney",
    "license": "MIT",
    "module": "dist/FocusLocker.esm.js",
    "scripts": {
        "compile": "rollup -c",
        "build": "node ./build.js",
        "test": "jest -c jest.config.js",
        "prepublishOnly": "npm run test && npm run build",
        "example": "npm run build && npx http-server -o /example"
    },
    "devDependencies": {
        "@babel/plugin-transform-react-jsx": "^7.21.0",
        "@babel/preset-env": "^7.20.2",
        "@testing-library/preact": "^3.2.3",
        "jest": "^29.4.3",
        "jest-environment-jsdom": "^29.4.3",
        "preact": "^10.12.1",
        "rollup": "^1.0.2"
    }
}
